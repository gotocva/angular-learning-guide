import { SparkImgLoaderDirective } from './spark-img-loader.directive';

describe('SparkImgLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SparkImgLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
